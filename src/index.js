import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return (
        <div>
            <h1>Chào mừng đến với Frontend Tutorial!</h1>
            <p>Khám phá các framework frontend như React, Vue.js và Angular.</p>
        </div>
    );
};

ReactDOM.render(<App />, document.getElementById('app'));